Finished avatar and skin combination assets belong here.

The current MVP renders base avatars directly and registers future skins in code.
When the Concession Stand or seasonal unlocks are built, generated combinations
can be exported into this folder and referenced from `avatarCombinations`.
