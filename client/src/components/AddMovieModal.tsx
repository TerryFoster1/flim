// Add Movie Modal placeholder.
// Future role: let users save a movie quickly from conversation context into one or more playlists.
// TODO: Add form behavior only after movie and playlist APIs are implemented.

export interface AddMovieModalPlaceholderProps {
  targetPlaylistId?: string;
  architectureNote: "Modal shell for future save-to-playlist flow";
}
