// MovieMetadataPanel placeholder.
// Future role: display supporting movie metadata beside or below the poster-first card.
// Intended metadata: title, year, runtime, genres, provider summary, and watch status.
// TODO: No external movie database integration in scaffold.

export interface MovieMetadataPanelPlaceholderProps {
  movieId?: string;
  architectureNote: "Poster-supporting metadata shell";
}
