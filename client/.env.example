# TMDb credentials now belong on the server/Vercel project, not in browser VITE variables.
# Use TMDB_ACCESS_TOKEN or TMDB_API_KEY in the root/server environment.

# Legacy browser-side variables are intentionally left out to avoid exposing movie API secrets.
