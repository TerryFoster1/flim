// Users route placeholder.
// Future routes may cover profiles, public playlist ownership, and settings surfaces.
// TODO: Authentication is explicitly out of scope for this scaffold.

export const usersRoutePlaceholder = {
  namespace: "/api/users",
  implemented: false,
};
