// Movies repository placeholder.
// Future responsibility: isolate PostgreSQL reads/writes for movie records.
// TODO: No database implementation in scaffold.

export interface MoviesRepositoryPlaceholder {
  storage: "PostgreSQL placeholder";
}
