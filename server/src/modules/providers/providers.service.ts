// Providers service placeholder.
// Future responsibility: coordinate provider availability planning without exposing external integration details to route handlers.
// TODO: No provider lookup, normalization, caching, or business logic in scaffold.

export interface ProvidersServicePlaceholder {
  purpose: "Future streaming provider use-case orchestration";
}
