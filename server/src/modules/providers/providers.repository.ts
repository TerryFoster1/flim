// Providers repository placeholder.
// Future responsibility: isolate PostgreSQL reads/writes for Providers, MovieProviders, and MovieLinks.
// TODO: No database implementation in scaffold.

export interface ProvidersRepositoryPlaceholder {
  storage: "PostgreSQL placeholder";
}
