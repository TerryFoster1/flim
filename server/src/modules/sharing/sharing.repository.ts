// Sharing repository placeholder.
// Future responsibility: isolate PostgreSQL reads/writes for shares, followers, and collaborators.
// TODO: No database implementation in scaffold.

export interface SharingRepositoryPlaceholder {
  storage: "PostgreSQL placeholder";
}
