# Server Schemas

Placeholder folder for future request validation schemas.

Future rules:

- Define route-level request and response schemas before implementing handlers.
- Keep server validation schemas aligned with `shared/src/types.ts` and `shared/src/schemas.ts`.
- Do not add real business rules during scaffold work.
