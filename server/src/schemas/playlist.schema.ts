// Playlist request/response schema placeholder.
// TODO: Define runtime validation for playlist payloads before implementing `/api/playlists`.

export const playlistSchemaPlaceholder = {
  name: "PlaylistSchema",
  implementationStatus: "placeholder_only",
} as const;
