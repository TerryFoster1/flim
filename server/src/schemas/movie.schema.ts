// Movie request/response schema placeholder.
// TODO: Define runtime validation for movie payloads before implementing `/api/movies`.

export const movieSchemaPlaceholder = {
  name: "MovieSchema",
  implementationStatus: "placeholder_only",
} as const;
