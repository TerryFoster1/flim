// Provider request/response schema placeholder.
// TODO: Define runtime validation for provider availability and movie link payloads before implementing `/api/providers`.

export const providerSchemaPlaceholder = {
  name: "ProviderSchema",
  implementationStatus: "placeholder_only",
} as const;
