// API route registry placeholder.
// TODO: Mount module routers under `/api` when implementation begins.

export const apiRouteNamespaces = ["movies", "playlists", "users", "sharing", "recommendations", "providers", "roulette", "social"] as const;
