{
  "name": "flim-app",
  "private": true,
  "version": "0.0.0",
  "description": "Create, share, and discover movie playlists.",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "lint": "npm --workspace client run build",
    "verify:details-ui": "node scripts/verify-production-ui-click-path.mjs"
  },
  "dependencies": {
    "@types/web-push": "^3.6.4",
    "sharp": "^0.33.5",
    "web-push": "^3.6.7"
  }
}
